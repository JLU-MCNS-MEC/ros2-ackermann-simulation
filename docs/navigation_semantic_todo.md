# 100 平方米自主导航与语义地图 TODO

更新日期：2026-09-10。目标：在约 10 × 10 m 室内空间完成传感器建图、重定位、
目标点导航，并逐步支持物体语义目标导航。勾选仅代表列出的验收实际通过。

## 执行顺序

- [x] P0：检查当前架构、工作区、远程连接及本机 SLAM/Nav2 依赖。
- [x] P1：建立 100 平方米室内场景，包含通道及具有几何外观的桌椅、货架。
- [x] P2：贯通 `/cmd_vel_safe -> /drive -> Gazebo`，保留唯一控制发布权。
- [x] P3：接 EKF + Slam Toolbox 在线建图，独立建图扫描，隔离真值 TF。
- [x] P4：保存栅格地图与 pose graph，重启后定位，运行目标点导航。
- [ ] P5：实现可持久化语义地标及目标查询，生成物体旁的可达导航位姿。
- [ ] P6：启用同步 RGB-D，完成检测/深度投影/跨帧融合；人工标注与视觉观测分开记录。
- [ ] P7：验证端到端语义导航、未知目标、歧义目标、不可达目标和取消。
- [ ] P8：自主探索、倒车脱困、动态障碍及定位退化回归。
- [ ] P9：三维语义地图、闭环后语义重融合与开放词汇查询。

## 验收与范围

- 每阶段补充公共函数正常、边界及错误测试，运行相关构建和测试。
- 导航实际运行记录与源码测试分别记录；不把 launch 可加载写成导航已通过。
- 初版命名地标允许人工标注，但必须标记来源，不能称为自动视觉语义建图。
- 场景真值只用于评估，不用于定位、自动识别或在线语义目标坐标生成。
- SLAM 首先人工指定目标覆盖空间；自动探索另行验收。
- 关键阶段独立 commit 并 push 到功能分支；不自动合并未验收功能到 main。
- 三维与开放词汇功能必须在二维闭环后继续实施，并单独验证依赖及算力。

## 运行记录

- 2026-09-10：基线为 `4c4e51b`，工作区干净；远程读取正常；本机已安装
  ROS 2 Jazzy、Slam Toolbox 和 Nav2 AMCL。
- 2026-09-10：100 平方米场景启动通过；在线生成约 201 × 201 栅格地图；
  前两个 Nav2 目标实际成功（23.42 s、15.35 s），完整巡回仍在运行。
  首次失败为 SLAM TF 时间滞后，启用 `restamp_tf` 后前述目标通过。
- 2026-09-10：保存 203 × 203 传感器地图与 pose graph；重启 AMCL 后两目标成功；
  RGB-D 10 组同步与采集时刻 TF 检查通过，自动语义识别仍待完成。
